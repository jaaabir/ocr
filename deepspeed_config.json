{
  "train_micro_batch_size_per_gpu": 4,
  "train_batch_size": 16,
  "zero_optimization": {
    "stage": 2,
    "offload_optimizer": { "device": "none" },
    "offload_param": { "device": "none" }
  },
  "fp16": {
    "enabled": true
  }
}
